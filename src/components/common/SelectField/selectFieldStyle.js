import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  formControl: {
    // minWidth: '200px',
    // marginTop: '16px',
    // marginBottom: '8px',
  },
  input: {
    height: '40px',
  },
}));
